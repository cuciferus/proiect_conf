<%= form_for([@pacient, @diabet]) do |f| %>
  <% if @diabet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diabet.errors.count, "error") %> prohibited this diabet from being saved:</h2>

      <ul>
      <% @diabet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br />
    <%= f.date_select :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
