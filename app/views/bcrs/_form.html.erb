<%= form_for(@bcr) do |f| %>
  <% if @bcr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bcr.errors.count, "error") %> prohibited this bcr from being saved:</h2>

      <ul>
      <% @bcr.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br />
    <%= f.date_select :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
